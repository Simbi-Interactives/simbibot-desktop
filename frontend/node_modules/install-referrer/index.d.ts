import { IonicNativePlugin } from '@ionic-native/core';
/**
 * @name Install Referrer
 * @description
 * This plugin is used for get the information sent from Play Store after install app.
 *
 * @usage
 * ```typescript
 * import { InstallReferrer } from '@ionic-native/install-referrer';
 *
 *
 * constructor(private installReferrer: InstallReferrer) { }
 *
 * ...
 *
 *
 * this.installReferrer.getReferrer()
 *   .then(referrer => console.log(referrer))
 *   .catch(error => console.error(error));
 *
 * ```
 */
export declare class InstallReferrer extends IonicNativePlugin {
    /**
     * This method return all parameters received in array 
     * @return {Promise<any>}
     */
    getReferrer(): Promise<any>;
}
